public class Triangle {
  public static void main(String[] args) {
  Turtle triangle = new Turtle();
  //triangle.left(90);
  //triangle.forward(100);
  //triangle.backward(100);
  //triangle.right(90);
  //triangle.forward(100);
  //triangle.right(45);
  //triangle.backward(141.422);

//  for (int i = 0; i < 3 ; i++) {
  //  triangle.backward(100);
  //  triangle.right(120);
  // }
  //Triangle method below
//  for(int j = 0; j < 3 ; j++)
  //  {
    //for ( int b = 0 ; b < 5 ; b ++) {

    // for (int i = 0; i < 4; i++) {
      //  triangle.backward(30);
        //triangle.right(120);
      //}
      //triangle.penup();
      //triangle.left(120);
      //triangle.forward(70);
      //triangle.pendown();
    //}
    // triangle.penup();
    // triangle.backward(200);
    // triangle.left(90);
    // triangle.forward(30);
     //triangle.right(90);
    // triangle.pendown();
//   }
// Square method
 for(int j = 0; j < 3 ; j++)
  {
 for ( int b = 0 ; b < 5 ; b ++) {
   for (int i = 0; i < 4; i++) {
      triangle.forward(40);
      triangle.left(90);
      }
      triangle.penup();
      triangle.forward(100);
      triangle.pendown();
    }
    triangle.penup();
    triangle.backward(500);
    triangle.left(90);
    triangle.forward(60);
    triangle.right(90);
    triangle.pendown()
}
}
